using AlexHolyGun;
using System.Collections.Generic;

Position position = new();

/*
decimal priceHi = ReadDcm("Задайте верхнюю цену: ");
decimal priceLow = ReadDcm("Задайте нижнюю цену: ");
decimal priceStep = ReadDcm("Задайте шаг цены: ");

List<Level> LevelList = Level.GetLevels(priceHi, priceLow, priceStep);

WriteList(LevelList);


void WriteList(List<Level> levels)
{
    Console.WriteLine($"Количество элементов в списке: {levels.Count.ToString()}");
    foreach (var level in levels) Console.Write(level.PriceLevel.ToString() + "\t");
}
decimal ReadDcm(string promptStr)
{
    Console.Write(promptStr);
    return decimal.Parse(Console.ReadLine());
}
*/